import MdxLayout from "@/components/MDXLayout";
import WeekSchedule from "@/images/week-schedule.png";
import Heading from "@/components/Heading";
import Image from "next/image";
import IntlLink from "@/components/IntlLink";

export const metadata = {
  title: "Schedule",
  description: "Overview schedule for the FOSS4G 2024 Conference held in Belém, Brasil on 02-08 December 2024",
  image: WeekSchedule
};

export const Button = ({text, href}) => <IntlLink href={href}><button className=" border-f4g_red text-white bg-f4g_orange border rounded-lg py-2 px-3 text-lg md:text-4xl font-ubuntu mx-1 md:mx-3 my-3">{text}</button></IntlLink>

<div className="flex flex-col sm:flex-row items-center justify-center">
  <div className="sm:w-1/2 my-4 text-center">
    <div className="">
      <Button text="General Schedule" href="/general-schedule" />
      <Button text="Academic Schedule" href="/academic-schedule" />
      <Button text="Workshop Schedule" href="/workshop-schedule" />
      <Button text="Get it on F-Droid" href="https://f-droid.org/packages/app.smarzaro.foss4g2024.schedule"  target="_blank"/>
    </div>
  </div>
  <div className="sm:w-1/2">
    <div className="text-xl mt-4 mb-2 md:text-4xl text-center font-ubuntu"><Heading bubble>December 02-08, 2024</Heading></div>

    <Image placeholder="blur" src={WeekSchedule} />
  </div>
</div>

<div className="text-3xl mt-4 mb-2 md:text-7xl text-center md:mt-8 md:mb-4 font-ubuntu"></div>

export default function MDXPage({ children }) {
  return <MdxLayout metadata={metadata}>{children}</MdxLayout>


}

export async function getStaticProps() {
  return {
    props: {
      metadata
    },
  }
}