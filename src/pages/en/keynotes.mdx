import Committees from "@/data/committees.json";
import Image from "next/image";
import Heading from "@/components/Heading";
import MdxLayout from "@/components/MDXLayout";
import Keynotes from "@/components/Keynotes";
import { useRouter } from "next/router";

export const metadata = {
  title: "Keynotes",
  description:
    "Overview of the keynotes FOSS4G 2024 Conference held in Belém, Brasil on 02-08 December 2024, including the Local, Academic, Program, and Sponsor committees.",
};

<Heading color="red" bubble>
  {"Conference Keynotes"}
</Heading>

<div className="grid items-center justify-center grid-cols-1 sm:grid-cols-3 sm:gap-10">
  <Keynotes bioText="(click here to read biography)" />
</div>

export default function MDXPage({ children }) {
  return <MdxLayout metadata={metadata}>{children}</MdxLayout>

}
export async function getStaticProps() {
return {
props: {
metadata
},
}
}
