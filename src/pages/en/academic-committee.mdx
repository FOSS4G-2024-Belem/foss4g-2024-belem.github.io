import Committees from "@/data/committees.json";
import Heading from "@/components/Heading";
import Foss4gAr2 from "@/images/foss4g-ar/foss4g-ar2.jpg";


import PhotoBubble from "@/components/PhotoBubble";
import MdxLayout from "@/components/MDXLayout";

export const metadata = {
  title: "Conference Committees",
  description:
    "Overview of the various committees of FOSS4G 2024 Conference held in Belém, Brasil on 02-08 December 2024, including the Local, Academic, Program, and Sponsor committees.",
  image: Foss4gAr2,
};

<Heading bubble color="orange">Academic Committee</Heading>

<div className="mx-auto w-full max-w-[720px] my-4 6xl:my-8">
  ##### About the Academic Committee

  The Academic Committee is the backbone that ensures its excellence and relevance. This group of experts plays a fundamental role in ensuring the quality and academic rigor of the designated track. Their work goes beyond reviewing proposals and selecting speakers.
  
  They are responsible for ensuring that each presentation and session reflects the latest research, advances and trends in the field. The Academic Committee not only validates the content, but also encourages interdisciplinarity and diversity of perspectives, thus building a track that promotes and drives progress in the academic community.

</div>


<div className="grid grid-cols-2 sm:grid-cols-2 sm:space-x-1 mx-auto md:max-w-[600px]">
  {Committees["academic"].filter((person) => ["Chair", "Co-Chair"].includes(person["role"])).map((person) => (
    <PhotoBubble committee="academic" person={person} key={person["name"]} />
  ))}
</div>


<Heading color="red">Committee Members</Heading>

<div className="grid grid-cols-2 sm:grid-cols-5 sm:space-x-1">
  {Committees["academic"].filter((person) => ["Member Comitte"].includes(person["role"])).map((person) => (
    <PhotoBubble committee="academic" showRole={false} person={person} key={person["name"]} />
  ))}
</div>

<Heading color="red">Review Members</Heading>

<div className="grid grid-cols-3 sm:grid-cols-6 sm:space-x-1">
  {Committees["academic"].filter((person) => ["Member Review"].includes(person["role"])).map((person) => (
    <PhotoBubble committee="academic" showRole={false} person={person} key={person["name"]} />
  ))}
</div>

export default function MDXPage({ children }) {
  return <MdxLayout metadata={metadata}>{children}</MdxLayout>

}
export async function getStaticProps() {
return {
props: {
metadata
},
}
}
